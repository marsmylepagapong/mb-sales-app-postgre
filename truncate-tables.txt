DO
$$
DECLARE
    r RECORD;
BEGIN
    -- Truncate all tables in public schema without resetting identity sequences
    FOR r IN (SELECT tablename FROM pg_tables WHERE schemaname = 'public') LOOP
        EXECUTE 'TRUNCATE TABLE ' || quote_ident(r.tablename) || ' CASCADE';
    END LOOP;
END;
$$;
